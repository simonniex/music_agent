# 音频文案生成器技术文档

## 产品定位

面向普通创作者的工具型 Demo：上传音频后完成转写、上下文增强、内容解析和平台文案生成。

```text
音频读取 -> ASR 转写 -> 歌词/上下文增强 -> 内容解析 -> 文案生成 -> 平台结果拆分
```

用户模式只展示最终可用结果；开发者模式展示转写、步骤状态和调试信息。

## UI 模式

### 用户模式

- 上传音频
- 生成文案
- 悬浮处理状态按钮
- 朋友圈文案
- 小红书文案
- 短视频标题

默认不展示 Prompt、转写全文或底层调用细节。

### 开发者模式

- 音频解析
- 词/内容亮点
- 曲风/听感建议
- 转写预览
- 转写状态
- 生成状态
- 音频文件信息
- 各处理步骤的执行状态和说明

## 辅助输入

### 歌曲名 + 歌手

```text
歌曲名
歌手
```

用于补充上下文。后续可接入歌词搜索 API，与 ASR 粗转写对齐后输出标准文本。

### 用户偏好歌词

用户输入偏好的歌词片段，用于控制文案方向，例如强调副歌情绪或某句表达。

### 歌词图片

多张歌词截图、海报或 LRC 图片。系统通过 YT-VITA 提取歌词文本，并与 ASR 结果对齐。

```text
歌词图片 -> 多模态识别 -> 提取歌词 -> 写入标准文本
```

### 参考截图

播放器截图、歌词页、评论区、专辑封面、短视频草稿等。用于补充歌名、歌手、评论情绪、封面氛围和平台风格，不默认替代歌词来源。

## 文本可信度优先级

多种文本来源并存时，优先级如下：

```text
用户手动粘贴歌词 / LRC
> 歌词图片识别
> 参考截图识别
> 歌曲名 + 歌手（ASR 为空时）
> ASR 转写
> 文件名/元信息推断
```

开发者模式显示标准文本来源；用户模式只显示标准文本预览和最终文案。

## 状态交互

状态区域为悬浮按钮式组件。

默认：整体状态、进度圆环、当前步骤说明。

展开后：逐步展示进度，完成后显示勾，每步可点「查看」。

- 「查看」：只显示该步骤详情。
- 「查看全部」：用户模式展示全部文案；开发者模式展示全部解析与调试信息。

## 处理流程

### 总体结构

核心业务在 KMP `shared` 层的 `AudioCopywritingGateway` 中编排：

```text
AudioInput
-> LightweightQualityAnalyzer（质量信号）
-> 并行：TencentAsrClient + YoutuVitaClient
-> LyricTextResolver（标准文本）
-> CopywritingPromptBuilder
-> DeepSeekClient（单次 LLM 调用）
-> Markdown 解析 -> 平台文案分区
```

UI 中的 Insight、Copywriting、Publish 等步骤是展示层拆分，底层对应一次 DeepSeek 调用和本地 Markdown 解析，并非多个独立 LLM 服务并行。

### 系统/工具步骤

#### 音频读取

接收用户上传的音频，读取元信息并构造 `AudioInput`。

工具：Android `ContentResolver`、`OpenableColumns`、MIME 类型读取、本地 bytes 读取。

输出：文件名、大小、MIME、Uri、音频 bytes。

#### 腾讯云 ASR 转写

将音频中的语音内容转成文本。

工具：腾讯云 `CreateRecTask`、`DescribeTaskStatus`、TC3-HMAC-SHA256 签名。

输出：转写文本、ASR 状态、音频时长、错误信息。

#### YT-VITA 图片识别

对歌词图片和参考截图做多模态识别。

工具：腾讯 TokenHub YT-VITA API。

输出：歌词片段、歌名、歌手、截图上下文。

### 逻辑模块

以下模块在代码和 UI 中按步骤展示，共用同一个 LLM（`deepseek-v4-flash`），差异在于输入上下文和输出用途。

#### LyricTextResolver

合并用户输入、视觉识别、歌名歌手、ASR 和文件名兜底，输出标准文本及来源标记。

#### Insight（展示步骤）

基于标准文本、用户偏好和图片上下文，生成主题、情绪、亮点和听感建议。实际由单次 LLM 调用的 Markdown 前半段承载。

#### Copywriting（展示步骤）

将解析结果改写成可直接发布的中文社交文案。Prompt 由 `CopywritingPromptBuilder` 组装；失败时由 `CopywritingFallbackBuilder` 生成本地兜底文案。

#### Publish（展示步骤）

不再次调用 LLM。解析生成 Markdown 中的平台分区，投递到用户模式结果卡片。

## 当前能力

### 已完成

- Android / Desktop 真实文件选择
- 腾讯云 ASR 真实转写
- 腾讯 MaaS DeepSeek-V4-Flash 真实生成
- YT-VITA 歌词图片与参考截图识别
- 用户模式 / 开发者模式切换
- 单步查看与查看全部
- 处理状态进度圆环
- 歌曲名、歌手、偏好歌词作为辅助上下文
- 多张歌词图片与参考截图，并区分图片归属
- 大文件本地分片 ASR（约 4MB/片）